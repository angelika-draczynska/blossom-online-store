import React from 'react';
import PropTypes from 'prop-types';
import styles from './NotFound.module.scss';

const NotFound = () => <div className={styles.root}></div>;

NotFound.propTypes = {};
export default NotFound;
